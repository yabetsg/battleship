export class Player{
    constructor(){ 
        this.turn = true;
        this.position = this.position;
    }
  
    autoPlay(){
        let random = Math.floor(Math.random()*10);
        return random;
    }
}