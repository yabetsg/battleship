export default class Player {
  constructor() {
    this.turn = true;
    this.position = this.position;
  }

  autoPlay(grid) {
    const random = Math.floor(Math.random() * 100);

    return grid[random];
  }
}
